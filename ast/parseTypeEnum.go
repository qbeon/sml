package ast

import "fmt"

// parseTypeEnum parses an enumeration type declaration statement
func parseTypeEnum(
	ctx *contextFile,
	crs Cursor,
) (*DeclEnum, error) {
	//TODO: implement
	return nil, fmt.Errorf("todo")
}
